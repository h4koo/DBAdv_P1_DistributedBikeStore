import { IncomeReportLine } from './income-report-line.model';

describe('IncomeReportLine', () => {
  it('should create an instance', () => {
    expect(new IncomeReportLine()).toBeTruthy();
  });
});
