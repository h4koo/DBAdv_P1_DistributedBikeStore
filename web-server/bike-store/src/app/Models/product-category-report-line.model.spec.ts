import { ProductCategoryReportLine } from './product-category-report-line.model';

describe('ProductCategoryReportLine', () => {
  it('should create an instance', () => {
    expect(new ProductCategoryReportLine()).toBeTruthy();
  });
});
