import { StoreProductsReportLine } from './store-products-report-line.model';

describe('StoreProductsReportLine', () => {
  it('should create an instance', () => {
    expect(new StoreProductsReportLine()).toBeTruthy();
  });
});
