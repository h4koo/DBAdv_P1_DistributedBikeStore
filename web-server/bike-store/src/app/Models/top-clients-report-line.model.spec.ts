import { TopClientsReportLine } from './top-clients-report-line.model';

describe('TopClientsReportLine', () => {
  it('should create an instance', () => {
    expect(new TopClientsReportLine()).toBeTruthy();
  });
});
